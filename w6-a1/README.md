# Iris Dataset
This script loads the Iris dataset using `scikit-learn`, converts it into a `pandas` DataFrame, and performs basic data cleaning and inspection.

## Calculating Correlation Among Features
